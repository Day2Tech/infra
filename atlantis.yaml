version: 3
projects:
- dir: .
  autoplan:
    when_modified: ["manifests/**/*.yaml", "*.tf*", "manifests/**.yaml"]
  workflow: lfs
workflows:
  lfs:
    plan:
      steps:
        - run: git lfs install
        - run: git lfs pull
        - init
        - plan